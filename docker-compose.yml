version: '3.8'
services:
  media-player:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - SECRET_KEY=your-secret-key-change-me
    restart: unless-stopped
